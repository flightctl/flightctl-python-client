# coding: utf-8

"""
    Flight Control API

    [Flight Control](https://flightctl.io) is a service for declarative management of fleets of edge devices and their workloads. 

    The version of the OpenAPI document: v1beta1
    Contact: team@flightctl.io
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import json
import pprint
from pydantic import BaseModel, ConfigDict, Field, StrictStr, ValidationError, field_validator
from typing import Any, List, Optional
from flightctl.models.aap_provider_spec import AapProviderSpec
from flightctl.models.k8s_provider_spec import K8sProviderSpec
from flightctl.models.o_auth2_provider_spec import OAuth2ProviderSpec
from flightctl.models.oidc_provider_spec import OIDCProviderSpec
from flightctl.models.open_shift_provider_spec import OpenShiftProviderSpec
from pydantic import StrictStr, Field
from typing import Union, List, Set, Optional, Dict
from typing_extensions import Literal, Self

AUTHPROVIDERSPEC_ONE_OF_SCHEMAS = ["AapProviderSpec", "K8sProviderSpec", "OAuth2ProviderSpec", "OIDCProviderSpec", "OpenShiftProviderSpec"]

class AuthProviderSpec(BaseModel):
    """
    AuthProviderSpec
    """
    # data type: OIDCProviderSpec
    oneof_schema_1_validator: Optional[OIDCProviderSpec] = None
    # data type: OAuth2ProviderSpec
    oneof_schema_2_validator: Optional[OAuth2ProviderSpec] = None
    # data type: OpenShiftProviderSpec
    oneof_schema_3_validator: Optional[OpenShiftProviderSpec] = None
    # data type: AapProviderSpec
    oneof_schema_4_validator: Optional[AapProviderSpec] = None
    # data type: K8sProviderSpec
    oneof_schema_5_validator: Optional[K8sProviderSpec] = None
    actual_instance: Optional[Union[AapProviderSpec, K8sProviderSpec, OAuth2ProviderSpec, OIDCProviderSpec, OpenShiftProviderSpec]] = None
    one_of_schemas: Set[str] = { "AapProviderSpec", "K8sProviderSpec", "OAuth2ProviderSpec", "OIDCProviderSpec", "OpenShiftProviderSpec" }

    model_config = ConfigDict(
        validate_assignment=True,
        protected_namespaces=(),
    )


    discriminator_value_class_map: Dict[str, str] = {
    }

    def __init__(self, *args, **kwargs) -> None:
        if args:
            if len(args) > 1:
                raise ValueError("If a position argument is used, only 1 is allowed to set `actual_instance`")
            if kwargs:
                raise ValueError("If a position argument is used, keyword arguments cannot be used.")
            super().__init__(actual_instance=args[0])
        else:
            super().__init__(**kwargs)

    @field_validator('actual_instance')
    def actual_instance_must_validate_oneof(cls, v):
        instance = AuthProviderSpec.model_construct()
        error_messages = []
        match = 0
        # validate data type: OIDCProviderSpec
        if not isinstance(v, OIDCProviderSpec):
            error_messages.append(f"Error! Input type `{type(v)}` is not `OIDCProviderSpec`")
        else:
            match += 1
        # validate data type: OAuth2ProviderSpec
        if not isinstance(v, OAuth2ProviderSpec):
            error_messages.append(f"Error! Input type `{type(v)}` is not `OAuth2ProviderSpec`")
        else:
            match += 1
        # validate data type: OpenShiftProviderSpec
        if not isinstance(v, OpenShiftProviderSpec):
            error_messages.append(f"Error! Input type `{type(v)}` is not `OpenShiftProviderSpec`")
        else:
            match += 1
        # validate data type: AapProviderSpec
        if not isinstance(v, AapProviderSpec):
            error_messages.append(f"Error! Input type `{type(v)}` is not `AapProviderSpec`")
        else:
            match += 1
        # validate data type: K8sProviderSpec
        if not isinstance(v, K8sProviderSpec):
            error_messages.append(f"Error! Input type `{type(v)}` is not `K8sProviderSpec`")
        else:
            match += 1
        if match > 1:
            # more than 1 match
            raise ValueError("Multiple matches found when setting `actual_instance` in AuthProviderSpec with oneOf schemas: AapProviderSpec, K8sProviderSpec, OAuth2ProviderSpec, OIDCProviderSpec, OpenShiftProviderSpec. Details: " + ", ".join(error_messages))
        elif match == 0:
            # no match
            raise ValueError("No match found when setting `actual_instance` in AuthProviderSpec with oneOf schemas: AapProviderSpec, K8sProviderSpec, OAuth2ProviderSpec, OIDCProviderSpec, OpenShiftProviderSpec. Details: " + ", ".join(error_messages))
        else:
            return v

    @classmethod
    def from_dict(cls, obj: Union[str, Dict[str, Any]]) -> Self:
        return cls.from_json(json.dumps(obj))

    @classmethod
    def from_json(cls, json_str: str) -> Self:
        """Returns the object represented by the json string"""
        instance = cls.model_construct()
        error_messages = []
        match = 0

        # use oneOf discriminator to lookup the data type
        _data_type = json.loads(json_str).get("providerType")
        if not _data_type:
            raise ValueError("Failed to lookup data type from the field `providerType` in the input.")

        # check if data type is `AapProviderSpec`
        if _data_type == "aap":
            instance.actual_instance = AapProviderSpec.from_json(json_str)
            return instance

        # check if data type is `K8sProviderSpec`
        if _data_type == "k8s":
            instance.actual_instance = K8sProviderSpec.from_json(json_str)
            return instance

        # check if data type is `OAuth2ProviderSpec`
        if _data_type == "oauth2":
            instance.actual_instance = OAuth2ProviderSpec.from_json(json_str)
            return instance

        # check if data type is `OIDCProviderSpec`
        if _data_type == "oidc":
            instance.actual_instance = OIDCProviderSpec.from_json(json_str)
            return instance

        # check if data type is `OpenShiftProviderSpec`
        if _data_type == "openshift":
            instance.actual_instance = OpenShiftProviderSpec.from_json(json_str)
            return instance

        # deserialize data into OIDCProviderSpec
        try:
            instance.actual_instance = OIDCProviderSpec.from_json(json_str)
            match += 1
        except (ValidationError, ValueError) as e:
            error_messages.append(str(e))
        # deserialize data into OAuth2ProviderSpec
        try:
            instance.actual_instance = OAuth2ProviderSpec.from_json(json_str)
            match += 1
        except (ValidationError, ValueError) as e:
            error_messages.append(str(e))
        # deserialize data into OpenShiftProviderSpec
        try:
            instance.actual_instance = OpenShiftProviderSpec.from_json(json_str)
            match += 1
        except (ValidationError, ValueError) as e:
            error_messages.append(str(e))
        # deserialize data into AapProviderSpec
        try:
            instance.actual_instance = AapProviderSpec.from_json(json_str)
            match += 1
        except (ValidationError, ValueError) as e:
            error_messages.append(str(e))
        # deserialize data into K8sProviderSpec
        try:
            instance.actual_instance = K8sProviderSpec.from_json(json_str)
            match += 1
        except (ValidationError, ValueError) as e:
            error_messages.append(str(e))

        if match > 1:
            # more than 1 match
            raise ValueError("Multiple matches found when deserializing the JSON string into AuthProviderSpec with oneOf schemas: AapProviderSpec, K8sProviderSpec, OAuth2ProviderSpec, OIDCProviderSpec, OpenShiftProviderSpec. Details: " + ", ".join(error_messages))
        elif match == 0:
            # no match
            raise ValueError("No match found when deserializing the JSON string into AuthProviderSpec with oneOf schemas: AapProviderSpec, K8sProviderSpec, OAuth2ProviderSpec, OIDCProviderSpec, OpenShiftProviderSpec. Details: " + ", ".join(error_messages))
        else:
            return instance

    def to_json(self) -> str:
        """Returns the JSON representation of the actual instance"""
        if self.actual_instance is None:
            return "null"

        if hasattr(self.actual_instance, "to_json") and callable(self.actual_instance.to_json):
            return self.actual_instance.to_json()
        else:
            return json.dumps(self.actual_instance)

    def to_dict(self) -> Optional[Union[Dict[str, Any], AapProviderSpec, K8sProviderSpec, OAuth2ProviderSpec, OIDCProviderSpec, OpenShiftProviderSpec]]:
        """Returns the dict representation of the actual instance"""
        if self.actual_instance is None:
            return None

        if hasattr(self.actual_instance, "to_dict") and callable(self.actual_instance.to_dict):
            return self.actual_instance.to_dict()
        else:
            # primitive type
            return self.actual_instance

    def to_str(self) -> str:
        """Returns the string representation of the actual instance"""
        return pprint.pformat(self.model_dump())


